let nums = [3.14, 42, 4811];

// TODO: Write a mapping function
// and pass it to .map()
let halved = nums.map(function(n)
{
    return Number(n) / 2;
});

console.log(nums);
console.log(halved);
